# Bundled deconwolf binaries (pre-built, works out of the box)
#
#   bin/linux-x86_64/dw        + lib/*.so
#   bin/windows-x86_64/dw.exe  + *.dll
#
# Binaries are patched to find bundled libraries via rpath ($ORIGIN/lib).
# Fallback: _get_library_env() in core.py sets LD_LIBRARY_PATH / PATH.
